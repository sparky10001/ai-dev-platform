###################################################################
# AI Dev Platform — LiteLLM Tiered Configuration (v2.0)
#
# Architecture:
#   fast     → local deterministic (Ollama)
#   balanced → NVIDIA + local fallback
#   heavy    → OpenAI + Anthropic + NVIDIA fallback
###################################################################

# ================================================================
# 🧠 LiteLLM Core
# ================================================================
LITELLM_PORT=4000
LITELLM_MASTER_KEY=ai-dev-platform

# ================================================================
# 🧠 Provider API Keys (Tier Backends)
# ================================================================

# --- OpenAI (heavy tier primary) ---
OPENAI_API_KEY=

# --- Anthropic Claude (heavy tier secondary) ---
ANTHROPIC_API_KEY=

# --- NVIDIA NIM (balanced + heavy fallback) ---
NVIDIA_NIM_API_KEY=

# ================================================================
# 🏠 Local Model Backend (fast tier)
# ================================================================
OLLAMA_BASE_URL=http://ollama:11434

# ================================================================
# ⏱️ Runtime Settings
# ================================================================
AI_TIMEOUT=120
AI_RETRIES=2
LITELLM_STARTUP_WAIT=60

# ================================================================
#  Resource Limits
# ================================================================
LITELLM_MEMORY_LIMIT=512m

# ================================================================
# 🧪 Dev / Debug
# ================================================================
AI_DEBUG=false
AI_ADAPTER=litellm

# ================================================================
# 📦 Optional Project Context
# ================================================================
ACTIVE_PROJECT=default

# ================================================================
# 🧠 Legacy Compatibility (safe to remove later)
# ================================================================
# These are ignored in new tier system but kept for migration safety
# MODEL_PROVIDER=litellm